services:
  mietspiegel-tool:
    build: .
    image: mietspiegel-tool:latest
    container_name: mietspiegel-tool
    restart: unless-stopped
    ports:
      # Host-Port anpassen, falls 5000 auf dem NAS belegt ist, z.B. "8091:5000"
      - "5000:5000"
